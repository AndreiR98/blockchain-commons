package uk.co.roteala.common.monetary;

import org.jetbrains.annotations.NotNull;

/**
 * After transaction was validated create BalancesDTO then process it
 * */
public class BalancesDTO implements Comparable<BalancesDTO>{
    @Override
    public int compareTo(@NotNull BalancesDTO o) {
        return 0;
    }
}
